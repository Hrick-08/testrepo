# Issue #1: Background color is too plain

**Body:**
The homepage background is currently plain white (`#ffffff`), which feels flat.
Please change the `body` background color in `style.css` to a dark navy,
`#1e1e2f`, so the page has a more modern look.

Make sure `pytest test_style.py` passes after your change, then open a PR.
